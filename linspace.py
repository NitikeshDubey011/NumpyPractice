from numpy import pi
import numpy as np


# where 0 is the start and 2 is the end and 9 is the number of elements which we want

print(np.linspace(0,2,9))

x=np.linspace(0,2*pi,100)
# print(x)

f=np.sin(x)
print(f)